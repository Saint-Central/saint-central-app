import { CourseEnrollment } from '../../types';

const reducer = (prev: CourseEnrollment[], action: { type: string; payload: CourseEnrollment }) => {
  // ... existing code ...
}

const reducer2 = (prev: CourseEnrollment[], action: { type: string; payload: CourseEnrollment }) => {
  // ... existing code ...
}

const reducer3 = (prev: CourseEnrollment[], action: { type: string; payload: CourseEnrollment }) => {
  // ... existing code ...
} 